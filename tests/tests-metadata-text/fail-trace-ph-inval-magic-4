/* CTF 1.8 */

trace {
  major = 1;
  minor = 8;
  byte_order = le;
  packet.header := struct {
    string a;
    integer {size = 32;} magic;
  };
};

stream {

};

event {
  name = event;

  fields := struct {
    string a;
  };
};
