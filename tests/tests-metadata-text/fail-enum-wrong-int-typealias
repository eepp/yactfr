/* CTF 1.8 */

trace {
  major = 1;
  minor = 8;
  byte_order = le;
};

stream {

};

typealias string := sss;

event {
  name = event;

  fields := struct {
    enum : sss {
      a, b, c, d,
    } en;
  };
};
