/* CTF 1.8 */

trace {
  major = 1;
  minor = 8;
  byte_order = le;
  packet.header := struct {
    integer { size = 32; signed = 1; } stream_id;
  };
};

stream {

};

event {
  name = event;

  fields := struct {
    string a;
  };
};
