/* CTF 1.8 */

trace {
  major = 1;
  minor = 8;
  byte_order = le;
};

stream {
  id = 1;
};

event {
  fields := struct {
    string a;
  };
};
----
[13:1] Event record type needs a data stream type (with ID 0) which doesn't exist.
