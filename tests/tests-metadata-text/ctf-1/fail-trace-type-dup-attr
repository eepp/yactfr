---
/* CTF 1.8 */

trace {
  major = 1;
  minor = 8;
  byte_order = le;
  minor = 8;
};

stream {

};

event {
  name = event;

  fields := struct {
    string a;
  };
};
---
[3:1] In `trace` root block:
[7:3] Duplicate attribute `minor`.
