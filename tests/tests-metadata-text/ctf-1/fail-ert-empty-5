/* CTF 1.8 */

trace {
  major = 1;
  minor = 8;
  byte_order = le;
};

stream {
};

event {
  fields := struct {
    struct {} s[0];
  };
};
----
[1:1] In the event record type 0 of data stream type 0:
[1:1] Any event record would be empty (no data).
