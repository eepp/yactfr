---
/* CTF 1.8 */

trace {
  major = 1;
  minor = 8;
  byte_order = le;
};

stream {

};

clock {
  name = salut;
  absolute = no;
};

event {
  name = event;

  fields := struct {
    string a;
  };
};
---
[13:1] In `clock` root block:
[15:14] Expecting `0`, `false`, `FALSE`, `1`, `true`, or `TRUE` for `absolute` attribute.
