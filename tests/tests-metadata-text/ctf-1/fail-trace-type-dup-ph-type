/* CTF 1.8 */

trace {
  major = 1;
  minor = 8;
  byte_order = le;
  packet.header := struct {
  };
  packet.header := struct {
  };
};

stream {
  id = 0;
};

stream {
  id = 1;
};

event {
  name = event;

  fields := struct {
    string a;
  };
};
