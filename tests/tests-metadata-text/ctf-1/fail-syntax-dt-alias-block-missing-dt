/* CTF 1.8 */

typealias := name;

trace {
  major = 1;
  minor = 8;
  byte_order = le;
};

stream {
};

event {
  fields := struct {
  	string a;
  };
};
----
[3:1] In data type alias:
[3:11] Expecting explicit data type block (`integer`, `floating_point`, `enum`, `string`, `struct`, or `variant`) or existing data type alias name.
