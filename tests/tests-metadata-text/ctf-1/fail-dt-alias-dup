/* CTF 1.8 */

typealias string := name;
typealias string := name;

trace {
  major = 1;
  minor = 8;
  byte_order = le;
};

stream {
};

event {
  fields := struct {
  	string a;
  };
};
----
[4:1] In data type alias:
[4:26] Duplicate data type alias: `name`.
