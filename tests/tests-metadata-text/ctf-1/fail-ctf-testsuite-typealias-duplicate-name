/* CTF 1.8 */

typealias integer { size = 32; align = 8; signed = false; } := uint32_t;

/* FAIL: duplicate name. */
typealias integer { size = 32; align = 8; signed = false; } := uint32_t;

trace {
	major = 1;
	minor = 8;
	byte_order = le;
	packet.header := struct {
		uint32_t magic;
	};
};


----
[6:1] In data type alias:
[6:73] Duplicate data type alias: `uint32_t`.
