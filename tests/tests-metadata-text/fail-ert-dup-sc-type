/* CTF 1.8 */

trace {
  major = 1;
  minor = 8;
  byte_order = le;
};

stream {
};

event {
  context := struct {
    string c;
  };

  fields := struct {
    string a;
  };

  context := struct {
    string z;
  };
};
